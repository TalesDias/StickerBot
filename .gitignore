# Env Vars
.env

# Dependencies
node_modules/
# package-lock.json is intentionally committed: baileys depends on libsignal via an
# unpinned git URL, so the lockfile is the only thing pinning that commit.

# IDE / Editor
.vscode/
.idea/

# TypeScript compiled output
dist/

# Local progress tracker
todolist.md

# Local planning docs — deliberately not tracked
MIGRATION.md
DISCUSSION.md
HANDOFF.md
